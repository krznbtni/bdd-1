// Import the built in assert module
let assert = require('assert');

// Import the GroceryListItem class
let GroceryListItem = require('./grocery-list-item');

// Collect all tests I want to do with
// the GroceryList class in a new class
module.exports = class GroceryListItemTest {

  constructor(){
    this.constructorTest();
  }

  constructorTest(){
  }

}